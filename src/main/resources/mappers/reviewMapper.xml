<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.video.model.dao.ReviewDao">
<!-- 	<resultMap type="Review" id="ReviewMap">
		<result column="reviewid" property="reviewid"/>
		<result column="title" property="title"/>
		<result column="writer" property="name"/>
		<result column="content" property="content"/>
		<result column="videoid" property="videoid"/>
	</resultMap> -->

	<!-- 전체글 조회 -->
	<select id="selectList" resultType="Review" parameterType="String">
		SELECT *
		FROM review WHERE videoid = #{videoid} ORDER BY viewCnt DESC;
	</select>

	<!-- 상세글 조회 -->
	<select id="selectOne" parameterType="int">
		SELECT *
		FROM review
		WHERE reviewid = #{reviewid};
	</select>

	<!-- 게시글 등록(서8 박정훈) -->
	<insert id="insertReview" parameterType="Review" keyProperty="reviewid" useGeneratedKeys="true">
		INSERT INTO Review (reviewid, title, writer, content, videoid)
		VALUES (#{reviewid}, #{title}, #{writer}, #{content}, #{videoid})
	</insert>
	
	<!-- 게시글 삭제 -->
	<delete id="deleteReview" parameterType="int">
		DELETE FROM review
		WHERE reviewid = #{reviewid}
	</delete>
	

	
	<!-- 게시글 수정 -->
	<!-- 컬럼이 등록 날짜만 관리를 하고 있다. -->
	<!-- 등록날짜 / 최근 조회날짜 / 수정 날짜 .... -->
	<update id="updateReview" parameterType="Review">
		UPDATE review
		SET title = #{title}, content = #{content}
		WHERE reviewid = #{reviewid}
	</update>
	
	<update id="updateViewCnt" parameterType="int">
	update review
	set `viewCnt` = viewCnt +1  
	where reviewid = #{reviewid}
	</update>

	
</mapper>